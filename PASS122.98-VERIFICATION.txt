PASS 122.98 — STATIC METADATA EXPORT FIX

GitHub Actions failure fixed:
- /manifest.webmanifest is explicitly force-static for Next.js output: export.
- /robots.txt is explicitly force-static.
- /sitemap.xml is explicitly force-static.
- /opengraph-image is explicitly force-static.
- GitHub Actions now audits all four metadata routes before the real build.
- The Autoprefixer warning at the loading-skeleton footer was cleaned from align-items:end to align-items:flex-end.
- Site UI and interaction behavior are unchanged from Pass 122.97.

Local sandbox validation:
- Static metadata audit: 4/4
- Workflow YAML syntax: PASS
- JavaScript syntax checks: PASS
- Existing non-build source audits: PASS where runnable without node_modules
- Full local Next.js build could not run because the sandbox cannot resolve registry.npmjs.org (EAI_AGAIN). The GitHub runner will execute the authoritative build after push.
