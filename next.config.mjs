const nextConfig = {
  reactStrictMode: true,
  devIndicators: false,
  poweredByHeader: false,
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
